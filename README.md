# @busyhe/ynn-plugin-cors

CORS请求处理插件。

## Installation

```sh
$npm install --save @busyhe/ynn-plugin-cors
```

## Usage

```js
const Ynn = require( 'ynn' );
const app = new Ynn( {
    plugins : {
        cors : {}
    }
} );
```
